# AGENTS.zh.md

这是给人阅读的中文说明版，便于团队快速理解本仓库对 Codex / AI 编码代理的默认要求。

机器侧主入口仍然是 [AGENTS.md](D:/GIT/inkscape_ZG/AGENTS.md)。

除非用户明确要求例外，否则默认遵循下面这些原则。

## 1. 先想，再写

- 不要默默猜测。
- 关键假设要先说出来。
- 需求有歧义时，先列出主要理解方式。
- 如果缺失信息会影响实现路径，先停下来澄清。
- 如果有更简单的方案，优先选更简单的。

## 2. 简单优先

- 只实现当前需求真正需要的最小代码。
- 不要顺手加新功能。
- 不要为一次性逻辑引入额外抽象。
- 不要提前加入没人要求的配置项和扩展点。
- 不要为不现实的场景添加想象中的错误处理。

## 3. 外科手术式修改

- 只改和当前请求直接相关的部分。
- 保持周围代码风格一致。
- 除非需求要求，否则不要顺手重构相邻代码。
- 不要顺手改注释、格式、命名或结构。
- 只清理由这次改动直接造成的无用代码。
- 看到无关问题可以提示，但不要顺手修。

每一行改动都应该能追溯到用户请求，或者追溯到本次验证需要。

## 4. 以可验证结果为目标

- 修 bug 时，能复现就先复现。
- 改行为时，先定义什么检查能证明改对了。
- 做重构时，验证前后行为一致。
- 多步骤任务要拆成简短步骤，并给每一步配验证点。

优先采用“先写失败检查，再修复，再让检查通过”的思路，而不是笼统地“把它搞定”。

## 5. 本仓库特别说明

这是一个基于 Inkscape 的代码仓库，并带有原生的 GRBL / AxiDraw 绘图流程扩展。

- 在 `src/axidraw/**` 和 `src/ui/dialog/**` 中优先做小范围、可测试的修改。
- 新增逻辑前，优先复用已有 helper、状态模块和已有工作流。
- 修改 GRBL 导出、传输、抬落笔、固件同步、分层暂停等流程时，同时考虑逻辑正确性和界面工作流影响。
- 行为变化尽量补最窄的相关测试到 `testfiles/`。

## 6. GRBL 参考路径

修改 GRBL 主机侧行为时，如串口流程、`ok` 协议处理、抬落笔控制、换刀流程、分层暂停、固件同步或相关 UI，可按需参考：

- 固件源码：`D:\GIT\inkscape-axidraw\Grbl_Esp32`
- 逆向得到的主机参考：`D:\GIT\kxnx`
- 主机/扩展参考仓库：`D:\GIT\inkscape-axidraw`
- 变体参考仓库：`D:\GIT\inkscape_px`
- 本仓库说明：`src/axidraw/FIRMWARE.md`

不要反复向用户索要这些路径。如果本机路径变化，应同步更新 `AGENTS.md` 和 `src/axidraw/FIRMWARE.md`。

## 7. 完成工作时的汇报

对非简单任务，结束时最好说明：

- 改了什么
- 验证了什么
- 还剩哪些不确定项，尤其是依赖真实硬件的部分
